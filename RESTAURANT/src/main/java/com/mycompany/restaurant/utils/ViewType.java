package com.mycompany.restaurant.utils;

/**
 * Defines all available views in the application for type-safe navigation
 */
public enum ViewType {
    DASHBOARD,
    TABLES,
    POS,
    KITCHEN,
    MENU,
    RESERVATIONS,
    INVENTORY,
    STAFF,
    ANALYTICS
}
